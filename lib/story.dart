class Story {
  String storyTitle; // Store the story text.
  String choice1; // Store the text for choice 1.
  String choice2; // Store the text for choice 2.

  Story({String storyTitle, String choice1, String choice2}) {
    this.storyTitle = storyTitle;
    this.choice1 = choice1;
    this.choice2 = choice2;
  }
}
